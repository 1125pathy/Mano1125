#include <stdio.h>
int main()
{
int v,p,a[100],sum=0;
printf("Enter the Length: ");
scanf("%d",&p);
for(v=0;v<n;v++)
scanf("%d",&a[v]);
for(v=0;v<p;v++)
sum=sum+a[v];
printf("The SUM is: %d",sum);
return 0;
}
